/**
 *  Authorization Module
 *
 *  A filler module which can be modified to provide authentication
 *  functionality to the websocket connection.
 */
define(function() {
    "use strict";

    return function(req, done) {
        done();
    };
});
